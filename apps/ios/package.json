{
  "name": "@cove/ios",
  "private": true,
  "scripts": {
    "build.skip": "xcodebuild -workspace ios/project.yml -scheme ios -configuration Release -sdk iphoneos -derivedDataPath build"
  },
  "version": "0.0.1"
}
